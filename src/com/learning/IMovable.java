package com.learning;

// ממשק הוא בעצם מעין חוזה / הגדרה של יכולת מסוימת
// מחלקה יורשת מחלקה - extends, מחלקה מממשת ממשק - implements
// ממשק מגדיר פעולות אשר מחייב את המחלקה המממשת את הממשק לממש את הפעולות הללו
// בממשק נגדיר אך ורק את החתימות של הפעולות
// פעולות הממשק חייבות להיות public, ניתן אף לא לרשום את המילה public בחתימה של הפעולה

// הגדרת ממשק בר תזוזה
public interface IMovable {
    void move();
}
